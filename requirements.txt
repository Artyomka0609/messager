# Основное приложение использует только стандартную библиотеку Python.
# psycopg2 нужен лишь если задана переменная DATABASE_URL
# (облачный PostgreSQL для сохранности данных, например Neon).
psycopg2-binary>=2.9
